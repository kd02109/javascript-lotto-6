/**
 * 수익률을 계산 해서 반환합니다.
 * @param {number} total
 * @param {number} money
 * @returns {number} rate
 */
const calcultePercent = (total, money) => (total / money) * 100;

export default calcultePercent;
